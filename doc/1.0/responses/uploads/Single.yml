description: A single upload
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          description: The HTTP status code
          type: number
          example: 200
        data:
          $ref: './../../schemas/uploads/Single.yml'
