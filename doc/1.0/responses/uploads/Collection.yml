description: List of uploads
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          description: The HTTP status code
          type: number
          example: 200
        data:
          $ref: './../../schemas/uploads/Collection.yml'
